# Exact continuation prompt for M6

```text
Use $proof-of-alpha at .agents/skills/proof-of-alpha/SKILL.md as the authoritative workflow and project context. Continue from the fully verified M5 Arc and CCTP technical lifecycle, including retained genuine Sepolia and Arc Testnet vault and bidirectional transfer evidence. Inspect Git status and preserve all existing changes, then read docs/project-state.md, all accepted ADRs, docs/milestones.json, docs/integrations.md, the M2 through M5 runbooks, and every skill reference required for evaluation, eligibility, commitments, Merkle proofs, exports, replay, API, web and testing. Consult the complete v0.3 source specification for missing or conflicting details. Run pnpm plan M6.

Your objective is M6 only: finish evaluation status separation, implement canonical append-only audit events and ordered Merkle commitment batches, deploy and publish the registry on Arc Testnet, build reproducible exports and independent replay/proof verification, and deliver the read-only hackathon dashboard over canonical backend results. Compliance, observed economics and statistical evidence must remain separate. Apply provenance exclusion before criteria: testnet, replay, synthetic and mixed results can never become real-capital eligible, and disabled inference can never produce eligibility. Preserve old reports and commitments through corrections.

Define one canonical leaf encoding and domain separation, deterministic event ordering, contiguous batch sequence ranges and predecessor chaining. Detect tampered leaves, gaps, overlaps, wrong predecessors, duplicate publication and reorged publications. A confirmed root requires an actual verified Arc Testnet registry deployment and publication receipt; periodic post-execution anchoring is an integrity guarantee and must never be described as independent proof that server receipt preceded execution. Keep publisher and agent signing keys separate and keep automatic funding disabled.

Export every schema and version, frozen policy/config/profile/adapter/parser hash, signed intent bytes, raw input descriptor, audit event, accounting/execution/transfer receipt, checkpoint, evaluation, batch, inclusion proof and registry receipt needed for a fresh process to reproduce a selected result. Missing objects, version drift and provenance splicing must fail closed. The dashboard must read canonical API objects and clearly show three correlated capital scenarios, both references, cash/positions/transit/blocked capital, costs, mark/liquidation, drawdown, descriptive differences, statuses, evidence provenance, incidents, limitations and proof verification without reimplementing accounting in the browser.

Satisfy every M6 observable exit in docs/milestones.json. Add meaningful eligibility/provenance, correction history, canonical encoding, Merkle proof, batch continuity, tamper/gap/overlap/predecessor/reorg, database restart, export, cross-process replay, API and UI tests. Add and run pnpm test:e2e:export-proof. Run all relevant validation, migrations, Foundry tests, replay, build and local service smoke; update generated schemas, versioned manifests, fact sheets, project state, integration documentation, demo material and ADRs; and make a focused verified M6 commit. Do not begin M7. Finish with the exact continuation prompt for M7.
```
