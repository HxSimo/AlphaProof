Use [$proof-of-alpha](/home/simo/Bureau/market_maker/AlphaProof/.agents/skills/proof-of-alpha/SKILL.md) at .agents/skills/proof-of-alpha/SKILL.md as the authoritative workflow and project context. Continue from the fully verified M6 evaluation, append-only commitments, Arc Testnet registry publications, reproducible export/proof replay and canonical read-only dashboard. Inspect Git status and preserve all existing changes, then read docs/project-state.md, all accepted ADRs, docs/milestones.json, docs/integrations.md, the M2 through M6 runbooks, docs/demo-runbook.md, and every skill reference required for demo hardening, monitoring, quotas, incidents, operations, deployment, replay and testing. Consult the complete v0.3 source specification for missing or conflicting details. Run pnpm plan M7.

Your objective is M7 only: harden and rehearse the complete hackathon demonstration. Add deterministic synthetic failure and stress demonstrations, a clearly labeled longer historical replay, and a separate fresh Ethereum mainnet-forward shadow session only if every existing Ethereum activation gate is genuinely satisfied. Preserve provenance boundaries so testnet, replay, synthetic and mixed evidence can never extend or upgrade the forward track record. Keep the three capital sizes labeled as correlated views of one policy, retain both frozen references, and keep automatic funding disabled.

Add the monitoring, quotas, incident display, operational recovery and deployment material needed to run the full path from external signer through durable prospective receipt, deterministic execution consequence, references, evaluation statuses, Arc commitment, export and independent proof replay. Rehearse delayed transfers, closure with unresolved transit, unavailable conservative reference, a successful withdrawal followed by failed swap, duplicate SDK submission, worker restart and a modified Merkle leaf. Every error, retry, cost, unavailable value, incident, unfavorable result and early stop must remain visible. A fallback recording or replay must retain its original date and provenance and must never be described as a fresh transaction.

Satisfy every M7 observable exit in docs/milestones.json. Add meaningful monitoring, quota, incident, recovery, provenance-splice, demo-path and deployment smoke tests. Run pnpm check, pnpm test:db, pnpm test:fork, pnpm test:replay, forge test --root contracts, pnpm test:e2e:export-proof and pnpm smoke, then perform and time the complete docs/demo-runbook.md rehearsal. Credentialed or public-network checks must fail clearly or skip only through their explicit documented gates; do not claim a fresh Ethereum session or external deployment when its evidence is unavailable. Update generated schemas, versioned manifests, fact sheets, project state, integration documentation, validation evidence, final README/demo/deployment material and ADRs; make a focused verified M7 commit. Stop after M7 and report the exact retained evidence, validation results, limitations and remaining TO_VERIFY dependencies.
