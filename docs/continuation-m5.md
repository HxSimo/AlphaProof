# Exact continuation prompt for M5

```text
Use $proof-of-alpha at .agents/skills/proof-of-alpha/SKILL.md as the authoritative workflow and project context. Continue from the verified M4 multi-capital and reference evaluation slice. Inspect Git status and preserve all existing changes, then read docs/project-state.md, all accepted ADRs, docs/milestones.json, docs/integrations.md, the M2 through M4 runbooks, and every skill reference required for Arc, CCTP, transfers, accounting, replay and testing. Consult the complete v0.3 source specification for missing or conflicting details. Run pnpm plan M5.

Your objective is M5 only: implement the Arc and CCTP technical lifecycle. Add the Sepolia and Arc Testnet ERC-4626 test-vault paths with a finite funded test-yield schedule frozen before experiment start; implement CCTP transfers in both directions with durable burn, finality, attestation, destination receipt and retry state; and integrate per-chain gas, decimal aliases and relayer funding without adding shadow bankroll.

Resolve current chain IDs, token contracts and decimals, CCTP domains and contracts, attestation behavior, fee mode, vault deployments, bytecode, roles, yield funding, finality and gas from official primary sources. M5 requires actual test-token vault deposit/withdraw and actual Sepolia→Arc Testnet and Arc Testnet→Sepolia transfers. Record genuine transaction receipts and content-addressed evidence only after verification. Never invent an address, deployment, balance, transaction hash, attestation, fee, route or successful live test.

Preserve the M1 transfer invariants across partial failures and restarts: one source debit, one unavailable receivable after burn, at most one destination credit, the same message identity on retries, and every actual cost exactly once. A timer alone cannot prove settlement. Delayed or missing attestations remain in transit; source failure releases the reservation minus incurred costs; destination failure remains retryable within the frozen bound; reorgs invalidate affected dependencies; and closure preserves unresolved capital without granting later yield retroactively.

Keep mainnet and economic-forward evidence separate from testnet technical demonstrations. Testnet, synthetic, replay and mixed results remain ineligible for real capital, and automatic funding remains disabled. If required RPCs, faucets, deployer/relayer keys or test tokens are unavailable, complete and test all deterministic interfaces, validation, fixtures and replay paths, keep each manifest gate `TO_VERIFY`, and document the exact command and evidence still required; do not mark M5 complete because mocks pass.

Satisfy every M5 observable exit in docs/milestones.json. Add meaningful unit, decimal/alias, gas, vault round-trip, transfer state-machine, duplicate-settlement, delayed-attestation, source-failure, destination-retry, reorg, closure, database restart, live-testnet and replay tests. Credentialed live tests must fail clearly or skip only through the explicit documented gate, and M5 completion requires the real testnet evidence. Run all relevant validation, migrations, Foundry tests and local service tests; update generated schemas, versioned manifests, fact sheets, project state and integration documentation; record material decisions as ADRs; and make a focused verified M5 commit. Do not begin M6. Finish with the exact continuation prompt for M6.
```
